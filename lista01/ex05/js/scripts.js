do
    nome = prompt("Digite seu nome completo: ");
while (nome.trim() == "")

var quantidadeCaracteres = nome.length;

document.write(`O nome informado possui ${quantidadeCaracteres} caracteres.`);

